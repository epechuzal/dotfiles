# Work-specific packages (DOTFILES_SETUP=work)
# Keep only things not already in the base or macos Brewfiles.
# Curate this list when you change jobs.
brew "tfenv"
brew "pipenv"
brew "awscli"
brew "openjdk"

cask "tuple"
cask "zoom"
cask "slack"
