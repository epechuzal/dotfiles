.idea/

git/gitconfig.local.symlink

**/Brewfile.lock.json

localrc.symlink

# Project-level Claude Code permissions (machine-local)
.claude/settings.local.json

# Machine-specific Hammerspoon overrides
hammerspoon/local.lua

# Work-specific config (stays local, not pushed to remote)
str/
